$(function() {
    $(document).on("click", "button#addFunction, button#addEmployee", function (e) {
        alert(3454567567);
        //url = $(this).data("url");
        //document.location.href = url;
    });

});